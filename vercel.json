{
  "buildCommand": "cd stinora-app && npm install && npm run build && cp dist/index.html ../index.html && cp -r dist/assets/* ../assets/ && cp dist/vite.svg ../vite.svg",
  "outputDirectory": "stinora-app/dist",
  "framework": "vite"
}
